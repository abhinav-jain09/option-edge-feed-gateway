- HIGH — [SpringConstructorResolutionTest.java:38](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/SpringConstructorResolutionTest.java:38): This is not a sufficient merge gate for a defect whose observable failure is “application context does not refresh.” `determineCandidateConstructors` checks only the annotation post-processor’s candidate discovery. It does not exercise bean-definition registration, constructor argument resolution, conditional configuration, `@Bean` methods, other post-processors, or actual instantiation. The repository already has a full-context test, [GatewayContextBootIT.java:16](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/mtsession/gateway/GatewayContextBootIT.java:16), but it is explicitly excluded from the normal suite and gated by external infrastructure. Add a default-suite context smoke test with external consumers/integrations disabled. That is the correct depth of guard.

- MEDIUM — [SpringConstructorResolutionTest.java:44](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/SpringConstructorResolutionTest.java:44): `candidates.size() >= 10` establishes only that scanning found something. The scan could silently lose a bean, subpackage, filter, or registration route while still finding far more than ten classes. It cannot support the test’s “every Spring-managed class” claim and does not cover imports, programmatic registrations, or factory-created beans. An actual context smoke test obtains the authoritative bean-definition set naturally.

- MEDIUM — [SpringConstructorResolutionTest.java:64](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/SpringConstructorResolutionTest.java:64): Unconditionally skipping abstract candidates can conceal valid abstract Spring components, notably abstract `@Lookup` beans that Spring subclasses. The interface skip is effectively redundant with normal component-candidate filtering; the abstract skip is a coverage hole.

- LOW — [SpringConstructorResolutionTest.java:29](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/SpringConstructorResolutionTest.java:29): The documentation says the test delegates to `BeanUtils.getResolvableConstructor`, but the code delegates to `AutowiredAnnotationBeanPostProcessor.determineCandidateConstructors`. The latter is the correct authority for discovering `@Autowired` constructor candidates, but it is not the complete container resolution process. The inaccurate explanation makes the test easier to over-trust.

The production changes themselves are correct: [FeedGatewayService.java:756](/private/tmp/oe-gw-ctor/src/main/java/app/feedgateway/FeedGatewayService.java:756) and [GatewayController.java:22](/private/tmp/oe-gw-ctor/src/main/java/app/feedgateway/GatewayController.java:22) unambiguously select the intended production constructors. The test will fail if either annotation is reverted: the specific assertion covers `FeedGatewayService`, and the component scan covers both classes and detects the absent no-arg fallback.

I found no other currently ambiguous Spring component in `src/main/java`; the other component classes with test-seam constructors already annotate their production constructors. All repository production sources are beneath `app.feedgateway`, matching the application’s implicit scan root. The audited configuration classes use factory methods, so Spring does not constructor-resolve their returned object types. I found no custom registrar, programmatic bean registration, or additional component-scan root. The recorded test result is two passing tests, but a fresh Maven execution was prevented by the worktree’s managed write restrictions.

VERDICT: REQUEST_CHANGES
