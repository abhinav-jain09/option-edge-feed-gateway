HIGH — [GatewayContextSmokeTest.java:49](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/GatewayContextSmokeTest.java:49) — The Kafka shutdown mechanism is not reliable. A Java static initializer is not guaranteed to run before `SpringExtension.beforeAll()` loads the context; reflective test discovery does not constitute active class use under the JLS. More decisively, [GatewaySettings.java:1788](/private/tmp/oe-gw-ctor/src/main/java/app/feedgateway/GatewaySettings.java:1788) gives `System.getenv("KAFKA_ENABLED")` precedence over the system property. Therefore an inherited `KAFKA_ENABLED=true` defeats line 51 and [FeedGatewayService.start()](/private/tmp/oe-gw-ctor/src/main/java/app/feedgateway/FeedGatewayService.java:1000) opens real Kafka consumers against the configured/default broker. A default-suite smoke test must establish isolation deterministically, not depend on the runner’s environment or JUnit class-initialization timing.

MEDIUM — [GatewayContextSmokeTest.java:54](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/GatewayContextSmokeTest.java:54) — Cleanup destroys process state and conflicts with Spring’s context cache. The test does not preserve a pre-existing `KAFKA_ENABLED` system property; it unconditionally clears it. Meanwhile `@SpringBootTest` contexts remain cached after `@AfterAll`, and `GatewaySettings` reads configuration lazily. Subsequent tests can receive the already-created, Kafka-disabled context after the property has reverted to its default—or observe changed behavior from the same settings bean. Add exact previous-value restoration and ensure the context is closed/evicted, or avoid global mutable configuration entirely.

MEDIUM — [GatewayContextSmokeTest.java:39](/private/tmp/oe-gw-ctor/src/test/java/app/feedgateway/GatewayContextSmokeTest.java:39) — “This one needs nothing” and “external integrations disabled” are unsupported. Only Kafka is addressed. Inherited feature/auth/database environment variables can activate Redis/Keycloak/Postgres-related wiring. `@SpringBootTest` uses a mock servlet environment, so it does not bind a network port, and the default configurations appear to avoid eager Postgres connections; however the test must explicitly pin every externally significant feature to a safe test profile rather than inherit arbitrary developer/CI/deployment configuration. Context construction also creates an actual temporary seller-activity directory through [SellerActivityDiskStore.java:24](/private/tmp/oe-gw-ctor/src/main/java/app/feedgateway/SellerActivityDiskStore.java:24).

The test is included in default `mvn test`: `GatewayContextSmokeTest` matches Surefire’s standard `*Test` convention, and the POM contains no exclusion. Reverting either production `@Autowired` independently makes its bean unconstructable: each affected class then has two unannotated constructors and no no-arg constructor, so context refresh fails before the assertions. I could not execute Maven in this sandbox because its read-only policy prevents writing `target/classes`; this is an environmental limitation, not evidence against that constructor-resolution conclusion.

Deleting the scanner loses breadth over components that are not instantiated in the default context—especially conditionally disabled configurations and lazy beans. That loss is real, although the removed approach did not prove full refresh and had already demonstrated semantic drift. The replacement is the correct kind of regression guard for this incident once its isolation is made deterministic.

VERDICT: REQUEST_CHANGES
